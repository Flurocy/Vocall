# Tasymize

Electron + React + TypeScript 桌面弹窗背词工具（Windows，全程本地，无后端）。

## 开发

```bash
npm install
npm run dev    # 启动开发模式，弹出管理窗口
npm run build  # 构建到 out/
npm test       # vitest
```

## 技术栈

Electron + electron-vite + React + TypeScript + Tailwind CSS + better-sqlite3 + Vitest
